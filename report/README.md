# Report (Assignment 2)

## Files

| File | Purpose |
|------|---------|
| `paper.tex` | 2-page report (fill matriculation, update v004 TPR when RunPod finishes) |

## Compile to PDF

**Overleaf:** upload `paper.tex`, compile with pdfLaTeX.

**Local (MiKTeX / TeX Live):**

```powershell
cd report
pdflatex paper.tex
pdflatex paper.tex
```

Output: `report/paper.pdf` — upload to CMS.

## Before submitting

1. Replace `[YOUR_NUMBER]` with your matriculation number.
2. Update Table 1 row **v004** when `submission_v002_logit_40k.csv` is submitted.
3. Trim **Planned next phases** if the PDF exceeds 2 pages (remove items you completed).
